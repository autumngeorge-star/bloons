apply plugin: "java"

sourceCompatibility = 8
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets {
    main {
        java {
            srcDir "src/"
            exclude "test/**"
        }
    }
    test {
        java {
            srcDir "src/test/java"
        }
    }
}
